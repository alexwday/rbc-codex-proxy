# OAuth Configuration
CLIENT_ID=your-client-id-here
CLIENT_SECRET=your-client-secret-here

# OAuth Token Endpoint (required - replace with your actual OAuth server)
TOKEN_URL=https://your-oauth-server.rbc.com/oauth/token

# Internal API Configuration (required - replace with your actual API endpoint)
# This is the endpoint that the OAuth token will be used to access
API_BASE_URL=https://your-internal-api.rbc.com/v1

# Proxy Configuration (optional - defaults shown)
PROXY_PORT=8080

# Model Parameters (optional - defaults shown)
# These will be applied to all requests unless overridden by Codex
MAX_TOKENS=32768        # Maximum tokens in response (increase if responses are cut off)
TEMPERATURE=0.2         # Model temperature (0.0-1.0, lower = more focused/deterministic)
TOP_P=1.0              # Nucleus sampling parameter
FREQUENCY_PENALTY=0     # Repetition penalty (-2.0 to 2.0)
PRESENCE_PENALTY=0      # Topic penalty (-2.0 to 2.0)

# Note: Place your rbc-ca-bundle.cer file in the certificates/ directory